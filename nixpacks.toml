# Nixpacks configuration for Railway
# Force Node.js 22 for Next.js 16

[phases.setup]
nixPkgs = ["nodejs_22", "bun", "openssl"]

[phases.install]
cmd = "bun install"

[phases.build]
cmd = "bunx prisma generate && bun run build"

[start]
cmd = "bun run start"

[variables]
NODE_ENV = "production"
